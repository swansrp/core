package com.bidr.insight.smartquery.service;

/**
 * Title: AssetModelingDiscipline
 * Description: 智能问数资产建模质量纪律（insight 业务层——内容含指标/关系/码值域/快照分区等
 * 数据仓库概念，属智能问数业务，不得下沉 llm 框架层）——
 * LLM 生成语义层资产（指标/关系/概念等）的质量契约：单位显式化、别名覆盖、快照纪律、
 * 命名语义化、口径完备、事实先行。由资产生成链（pipeline 探索链/自主会话链）的系统提示词拼接，
 * 与 llm 框架层 AutonomousSystemPrompt（规划纪律，管「怎么干活」）正交：本纪律管「资产产出成什么样」。
 *
 * @author Sharp
 * @since 2026/8/22
 */
public final class AssetModelingDiscipline {

    /** 资产建模质量纪律（业务段，由资产生成链系统提示词拼接） */
    public static final String MODELING_DISCIPLINE =
            "【资产建模质量纪律】产出的每一项资产必须满足以下质量契约：\n"
            + "一、单位显式化：金额/数量类指标的描述必须写明「单位：…」；"
            + "列备注标注的单位与实际采样量级不一致时（如备注为元、量级像万元），"
            + "必须用参考值交叉核实后按真实存储单位填写，并把核实依据写入描述；\n"
            + "二、别名覆盖：别名叫法覆盖口语、简称与变体（含带括注的简称写法），"
            + "保证用户以任何常见叫法提问都能命中该资产；\n"
            + "三、快照纪律：全量快照表的指标描述与关系备注必须写明"
            + "「聚合须限定单一分区（通常取最新）」，并提示快照表跨分区重复计数、"
            + "与明细表关联时的行数放大风险；\n"
            + "四、命名语义化：资产名语义自明，禁止用无意义后缀消重；"
            + "名称冲突时用区分业务含义的前缀；\n"
            + "五、口径完备：用户会用一个词自然问出的高频口径（如总额=多列之和），"
            + "必须生成独立指标或概念，禁止留给问数时现场拼凑；"
            + "同表组合指标的公式可为聚合的算术组合（如 SUM(db.tbl.a)+SUM(db.tbl.b)）；\n"
            + "六、事实先行：字段的存在/类型/单位/码值、join 键有效性一律先工具核实再产出资产，"
            + "描述与备注写「已核实的结论」，不写「可能/应该」；无法核实的疑点在总结中显式标注。";

    private AssetModelingDiscipline() {
    }
}

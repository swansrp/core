kafka:
  # Kafka服务器地址
  bootstrap-servers: localhost:9092
  
  # 安全配置 (SASL认证，如不需要可注释掉)
  security:
    # 安全协议: PLAINTEXT, SSL, SASL_PLAINTEXT, SASL_SSL
    # protocol: SASL_PLAINTEXT
    # SASL机制: PLAIN, SCRAM-SHA-256, SCRAM-SHA-512, GSSAPI
    # mechanism: SCRAM-SHA-256
    # SASL用户名
    # username: 
    # SASL密码
    # password: 
  
  # 生产者配置
  producer:
    # 发送确认模式: all, 1, 0
    acks: all
    # 发送失败重试次数
    retries: 3
    # 批量发送大小(字节)
    batch-size: 16384
    # 批量发送延迟时间(毫秒)
    linger-ms: 1
    # 发送缓冲区大小(字节)
    buffer-memory: 33554432
    # Key序列化器
    key-serializer: org.apache.kafka.common.serialization.StringSerializer
    # Value序列化器
    value-serializer: org.apache.kafka.common.serialization.StringSerializer
  
  # 消费者配置
  consumer:
    # 默认消费者组ID
    group-id: default-group
    # 是否自动提交偏移量
    auto-commit: false
    # 自动提交间隔(毫秒)
    auto-commit-interval: 1000
    # 偏移量重置策略: earliest, latest, none
    auto-offset-reset: latest
    # Key反序列化器
    key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    # Value反序列化器
    value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    # 单次poll最大记录数
    max-poll-records: 100
    # 会话超时时间(毫秒)
    session-timeout-ms: "30000"
    # 心跳间隔(毫秒)
    heartbeat-interval-ms: "10000"
  
  # 事务配置
  transaction:
    # 是否启用事务
    enabled: true
    # 事务ID前缀
    transaction-id-prefix: kafka-tx-${HOSTNAME}-

  # 死信队列配置
  dlq:
    # 是否启用死信队列（默认true）
    enabled: true
    # 死信队列后缀（原Topic为"test"，则死信队列为"test-dlq"）
    suffix: "-dlq"
    # 重试次数（默认3次）
    retry-attempts: 3
    # 重试间隔(毫秒)（默认1秒）
    retry-interval: 1000
    # 是否使用指数退避（默认false）
    exponential-backoff: false
    # 指数退避乘数（默认2.0）
    backoff-multiplier: 2.0
    # 最大重试间隔(毫秒)（默认30秒）
    max-retry-interval: 30000
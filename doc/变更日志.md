# core 仓变更日志

> 定位：按交付笔次记录 `server/framework/core` 的机制级改动（是什么 / 动了哪 / 怎么验）。
> 机制细节正源不在此处：不变式与交互矩阵见 dev-flow 留档（`.qoder/dev-logs/<session>/plan.md`），长期运维口径见 [Agent循环治理策略.md](Agent循环治理策略.md) §二 11/12 与 §六，设计动机与实测证据见 [机制巧思台账.md](机制巧思台账.md)。

---

## 2026-09-25 L1 上下文预算治理（工具结果入场卸载 + 句柄回捞 / 计数窗→token 预算双轨取严）

**状态：随本笔提交入库（本地 commit，未 push——框架 core 改动 push 前须经用户 review）**。
**边界（修正案 A6）**：本轮改动现场在本项目 core；项目侧（epc_ai 两链、InkHub）零改动，框架侧默认全关（I9）。InkHub `InkHubCheckAgent` 两 setter + 真模型四组对照（层 2）后置为独立实测轮，**层 2 数字全部 PENDING**。

### Added
- `llm/.../agent/ContextTokenEstimator.java` —— token 估算纯函数（CJK×1.0 + 其他×0.3 + 每条 4 + specs×1.2，只高不低，I8）
- `llm/.../agent/ToolResultOffloader.java` —— 入场卸载判定 + 指针格式化（句柄落首行前 60 字符，扛 brief 截断，I7；反向膨胀则放弃卸载）
- `llm/.../agent/AgentToolRecall.java` —— 内建回捞工具 `recallToolResult`（三闸：3 次/run、20000 字/次、结果不二次卸载）
- `llm/.../agent/AgentContextBudget.java` —— 五阈值三级取值单一出口（option 正值 > sys_config > Param 默认；开关键 `resolve` 0=关闭、闸门键 `resolveFloor` 0/负回落保守默认）
- `doc/机制巧思台账.md`（新建 2 条 × 六要素）、`doc/变更日志.md`（本文件）
- 测试 7 类 66 案 + 夹具 `ContextProbeTools` / `ScriptedModel`（层 1 合成对照，CI 可跑零外部依赖）

### Changed
- `ToolAgentRunner.java`（主战场）—— 三处工具结果入场收敛到 `appendToolResultMessage`（卸载/收口/钉住统一出口）；`trimToolMemory` 双轨取严（条数切点与 token 切点取 `Math.max`，切点回退避拆对唯一防线不变，I11）；计量日志行（条数/字符/估算token/本轮卸载/累计卸载/累计回捞/累计驱逐，仅开启态）；`RunStats` run() 局部容器守 I14；`effectiveTools` 承载回捞工具注册（specs 与 executeTool 全走它）
- `AgentLoopOptions.java` —— 纯追加 `contextTokenBudget` / `toolResultOffloadChars` 两字段（默认 0=关；3 个位置构造器一字未动）
- `AgentLoopListener.java` —— 新增 2 个 default：`supportsToolResultRecall()`（I5 判据）/ `recallToolResult(id)`
- `session/AgentSessionContext.java` —— 覆写回捞：只读扫本会话事件流按 `tool_call_id` 取全文（I13 单写者不破；sessionId 硬隔离有回归案）
- `constant/param/LlmParam.java` —— 新增 5 个 `AGENT_*` 键（两开关默认 `"0"`；三闸门键默认 1000/3/20000，remark 写明两类键 0 语义分野）
- `doc/Agent循环治理策略.md`（§二 11/12、§五 Token 项移出规划表、§六 灰度键与两类键语义）、`llm/README.md` §7.3（两开关用法示例）

### Impact
- 默认关闭态：全部 10 处既有 `new AgentLoopOptions(...)` 调用点行为与改造前**逐条一致**（等价性回归以改造前算法照抄为基线快照，逐轮全量签名比对）；计量走 log 不进事件流。
- 开启正门：代码级 setter 或运维参数页置 `AGENT_TOOL_RESULT_OFFLOAD_CHARS` / `AGENT_CONTEXT_TOKEN_BUDGET` 正值；卸载仅在回捞通道可用（会话链）时生效——"只卸载不回捞"组合被机制故意拒绝（I4/I5）。
- 三层回滚无需回码：sys_config 置 0 / 注释项目侧 setter / revert 本笔（不落库、不动 schema）。
- 验证：`mvn test -pl framework/core/llm -am` → llm 模块 `Tests run: 120, Failures: 0, Errors: 0`（权威 `surefire-reports/TEST-TestSuite.xml` 06:13:07，逐类求和一致）；末轮文本改动后 `ToolAgentRunnerTokenBudgetTest` 单类复验 10/0。层 1 对照（合成夹具 `ScriptedModel` 口径）：入窗峰值字符 120100 → 24859（降 79.3%），`KEY-*` 探针回捞 3/3。
- 管线：Plan-Checker 3 轮 PASS；Reviewer 3 轮（R1 REQUEST_CHANGES 3🟡 → A7、R2 APPROVE 1🟡 → A8、R3 APPROVE 🔴0/🟡0/🔵3）；I11 按计划文本 `min` 系笔误，取严为 `Math.max` 已经主 agent 裁定更正基准。

---

## 2026-09-25（第二笔）摘要行内句柄：被驱逐的工具结果从"失联"变为"可回捞"（`I15`，修正案 A9）

**状态：随本笔提交入库（本地 commit，未 push）**。承接上一笔 commit `6949a7f1`，独立成笔。

### 补的缺口
`digestOf` 生成的探索摘要行原本只留 `· 工具名(参数摘要) → 结果摘要`，**丢掉了 `tool_call_id`**。而回捞取数走 `AgentSessionContext.recallToolResult` 按 id 扫全部 TOOL_RESULT 事件、**与是否曾卸载无关**——即原文一条没少地躺在事件流里，只是句柄在裁切那一刻丢了。净效果是"窗内指针可回捞、裁进摘要即永久失联"，一个纯加法就能收口的缺口。

### Changed
- `ToolAgentRunner.java` —— ① 新增同源判据 `recallUsable = recallAvailable && (offloadOn || tokenBudget > 0)`，回捞工具注册（原条件 `offloadOn`）与摘要写句柄共用；② `digestOf` 加 `withHandles` 形参，命中工具结果的行尾追加 `（句柄=<tool_call_id>）`；③ 摘要头部在 `DIGEST_PREFIX` 之后、正文之前插一行回捞指引 `DIGEST_RECALL_GUIDANCE`，重拼时幂等剥离防重复（`DIGEST_PREFIX` 常量本身一字未动——它是关闭态也走的头部识别标记）。
- `AgentToolRecall.java` —— `@Tool` description 与 `@P` 参数说明扩到两个句柄来源（指针首行 `tool_call_id=` / 摘要行内 `句柄=`），"一次一个、禁止重复拉同一句柄"纪律措辞保留。
- `doc/Agent循环治理策略.md` §二 11/12、`doc/机制巧思台账.md` 条目 1（不变式补 I15、可主张点与代价栏更新）。

### Impact
- **I9 未受影响**：默认全关（`offloadChars=0 && tokenBudget=0`）时 `recallUsable=false` → specs 不增工具、digest 不写句柄、无指引行，逐字符与改造前一致；照抄 HEAD 算法的基线模拟回归案**一行未改仍通过**（测试文件 183 行纯新增、零删除）。
- 新开启形态：`AGENT_CONTEXT_TOKEN_BUDGET` 单开（不开卸载）即可让被裁内容可回捞——这是本笔的实际收益面。
- 代价：摘要行每挂一个句柄多占约 30-40 字符，挤压 `DIGEST_MAX_LEN=3000` 容量 → 同容量下可留存条数下降；模型可回捞的对象变多，由 `AGENT_TOOL_RECALL_MAX_PER_RUN`（默认 3）兜住。
- 验证：`mvn test -pl framework/core/llm -am` → llm 模块 **`Tests run: 124, Failures: 0, Errors: 0`**（权威 `TEST-TestSuite.xml` 07:06:13，逐类求和一致；较上笔 +4：`ToolAgentRunnerTokenBudgetTest` 10→13、`AgentToolRecallTest` 3→4）。新增四案：`recallUsable真值矩阵specs与digest同源`（6 格断言两处同源）、`多轮驱逐指令行唯一且各行带句柄`、`摘要内指针保留句柄可回捞`、`digest行内句柄端到端回捞逐字符等于原文`（本笔存在理由：不卸载→被裁→读句柄→取回全文逐字符相等）。
